<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Online Shopping Assistance Platform</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Online Shopping Assistance Platform</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/addOrder">Add new order</a>
            </li> 
          </ul>
        </div>
      </div>
    </nav>
  <div class="container">
    <br>
    <div class="text-center"><h2>Orders from Online Shopping Assistance Platform Database</h2></div>
    <br>
  
    <table class="table table-hover small text-center">
      <thead>
          <tr>
            <th width="100">Product Name</th>
            <th width="100">Product Image</th>
            <th width="50">Tracking Number</th>
            <th width="50">Website URL</th>
          </tr>
      </thead>
      <tbody>
        <% for(let i=0; i < orders.length; i++) { %>
          <tr>
            <td><a href="/order/<%= orders[i].orderId %>" ><%= orders[i].productName %></a></td>
            <td><img src="images/<%= orders[i].image %>" width="20%"></td>
            <td><%= orders[i].tracking_number %></td>
            <td><%= orders[i].website_url %></td>
            <!-- Edit Link -->
            <td><a href="/editOrder/<%= orders[i].orderId %>">Edit</a></td>
            <!-- Delete link -->
            <td><a href="/deleteOrder/ <%= orders[i].orderId %>" onclick="return confirm('Are you sure you want to delete this order?')">Delete</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>

<%# 23011077 ruien %>